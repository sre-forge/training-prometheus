[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "training-prometheus"
version = "0.1.0"
description = "Deterministic Prometheus environment profiler and skill compiler"
requires-python = ">=3.10"

[project.scripts]
training-prometheus = "training_prometheus.cli:main"
